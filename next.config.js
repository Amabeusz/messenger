/** @type {import('next').NextConfig} */
const nextConfig = {
    // async redirects() {
    //     return [
    //       {
    //         source: '/login/api/form',
    //         destination: '/chat', // Matched parameters can be used in the destination
    //         permanent: true,
    //       },
    //     ]
    //   },
}

module.exports = nextConfig
